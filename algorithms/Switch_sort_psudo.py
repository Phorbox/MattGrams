current = 0
while current < set length
    for each in set
        if each is < smallest 
            smallest = each
        switch current and smallest
    current++

Bubblesort(A)
    for i = 1 to a.length-1
        for j = a.length downto i+1
            if a[j] < a[j-1]
                exchange a[j] with a[j-1] 

# Made in Python
i = 0
for each in set
    if each is v
        return i
    else i++